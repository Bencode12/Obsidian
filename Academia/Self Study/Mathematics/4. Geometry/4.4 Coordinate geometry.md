### **4.4.1 What is coordinate geometry?**

Coordinate geometry represents **geometric figures on a plane** using **coordinates** (x,y)(x, y)(x,y).

- **X-axis** → horizontal line
- **Y-axis** → vertical line
- **Origin** → (0, 0), intersection of axes

### **4.4.2 Plotting points**

- Each point has coordinates (x, y)
- Move x units along X-axis, y units along Y-axis

Example: (3, 4) → 3 units right, 4 units up

### **4.4.3 Distance formula**

Distance between two points (x1,y1)(x_1, y_1)(x1,y1) and (x2,y2)(x_2, y_2)(x2,y2):

d=(x2−x1)2+(y2−y1)2d = \sqrt{(x_2 - x_1)^2 + (y_2 - y_1)^2}d=(x2−x1)2+(y2−y1)2

### **4.4.4 Midpoint formula**

Midpoint of segment connecting (x1,y1)(x_1, y_1)(x1,y1) and (x2,y2)(x_2, y_2)(x2,y2):

M=(x1+x22,y1+y22)M = \left( $\frac{x_1 + x_2}{2}$, $\frac{y_1 + y_2}{2}$ \right)M=(2x1+x2,2y1+y2)

### **4.4.5 Slope of a line**

Slope $m$ measures steepness:

m=y2−y1x2−x1m = $\frac{y_2 - y_1}{x_2 - x_1}$m=x2−x1y2−y1

- Positive → rises to the right
- Negative → falls to the right
- Zero → horizontal
- Undefined → vertical

### **4.4.6 Equation of a line**

1. **Slope-intercept form**: y=mx+cy = mx + cy=mx+c
    - m = slope, c = y-intercept
2. **Point-slope form**: y−y1=m(x−x1)y - y_1 = m(x - x_1)y−y1=m(x−x1)