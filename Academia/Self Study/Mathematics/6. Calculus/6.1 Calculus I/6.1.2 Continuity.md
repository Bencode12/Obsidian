### **What does continuity mean?**

A function is **continuous at a point** if its graph can be drawn **without lifting the pen** at that point.

Formally, a function f(x)f(x)f(x) is **continuous at x=ax = ax=a** if **all three** of the following conditions hold:

1. **f(a)f(a)f(a) is defined**
2. **lim⁡x→af(x)\lim_{x \to a} f(x)limx→a​f(x) exists**
3. **lim⁡x→af(x)=f(a)\lim_{x \to a} f(x) = f(a)limx→a​f(x)=f(a)**

If **any one** fails, the function is **not continuous** at x=ax = ax=a.

---

### **Types of discontinuities**

#### **1. Removable discontinuity**

- A “hole” in the graph
- Limit exists, but f(a)f(a)f(a) is missing or incorrect

Example:

f(x)={x2−1x−1,x≠15,x=1f(x) = \begin{cases} \frac{x^2 - 1}{x - 1}, & x \neq 1 \\ 5, & x = 1 \end{cases}f(x)={x−1x2−1​,5,​x=1x=1​ lim⁡x→1f(x)=2≠f(1)\lim_{x \to 1} f(x) = 2 \neq f(1)x→1lim​f(x)=2=f(1)

---

#### **2. Jump discontinuity**

- Left-hand and right-hand limits exist but are **not equal**

Example:

f(x)={1,x<03,x≥0f(x) = \begin{cases} 1, & x < 0 \\ 3, & x \ge 0 \end{cases}f(x)={1,3,​x<0x≥0​

---

#### **3. Infinite discontinuity**

- Function blows up to ±∞

Example:

f(x)=1xat x=0f(x) = \frac{1}{x} \quad \text{at } x = 0f(x)=x1​at x=0

---

### **Continuity on an interval**

- Continuous on (a,b)(a,b)(a,b) → continuous at every point inside
- Continuous on [a,b][a,b][a,b] → continuous inside + right/left continuity at endpoints

---

### **Continuity of common functions**

The following are continuous **wherever they are defined**:

- Polynomials
- Rational functions (except where denominator = 0)
- Exponential and logarithmic functions
- Trigonometric functions

---

### **Important theorems (conceptual)**

You will use these later heavily.

#### **Intermediate Value Theorem (IVT)**

If f(x)f(x)f(x) is continuous on [a,b][a,b][a,b], then for any value kkk between f(a)f(a)f(a) and f(b)f(b)f(b), there exists some c∈(a,b)c \in (a,b)c∈(a,b) such that:

f(c)=kf(c) = kf(c)=k

This guarantees **existence of solutions**.