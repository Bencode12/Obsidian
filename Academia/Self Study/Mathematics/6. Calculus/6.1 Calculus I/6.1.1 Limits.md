### **6.1.1 Limits**

### **What is a limit?**

A **limit** describes the value that a function **approaches** as the input approaches a certain number.

Notation:

lim⁡x→af(x)=L\lim_{x \to a} f(x) = Lx→alim​f(x)=L

This means: as xxx gets closer to aaa, f(x)f(x)f(x) gets closer to LLL.

---

### **Intuitive understanding**

- The limit is about **behavior near a point**, not necessarily at the point.
- A function may have a limit even if:
    - f(a)f(a)f(a) is undefined
    - f(a)f(a)f(a) ≠ limit

Example:

f(x)=x2−1x−1f(x) = \frac{x^2 - 1}{x - 1}f(x)=x−1x2−1​

At x=1x = 1x=1, function is undefined.  
But:

lim⁡x→1x2−1x−1=lim⁡x→1(x+1)=2\lim_{x \to 1} \frac{x^2 - 1}{x - 1} = \lim_{x \to 1} (x + 1) = 2x→1lim​x−1x2−1​=x→1lim​(x+1)=2

---

### **One-sided limits**

- **Left-hand limit**:

lim⁡x→a−f(x)\lim_{x \to a^-} f(x)x→a−lim​f(x)

- **Right-hand limit**:

lim⁡x→a+f(x)\lim_{x \to a^+} f(x)x→a+lim​f(x)

The limit exists **only if both are equal**.

---

### **Basic limit laws**

If limits exist:

1. lim⁡(f(x)+g(x))=lim⁡f(x)+lim⁡g(x)\lim (f(x) + g(x)) = \lim f(x) + \lim g(x)lim(f(x)+g(x))=limf(x)+limg(x)
2. lim⁡(cf(x))=clim⁡f(x)\lim (cf(x)) = c \lim f(x)lim(cf(x))=climf(x)
3. lim⁡(f(x)g(x))=(lim⁡f(x))(lim⁡g(x))\lim (f(x)g(x)) = (\lim f(x))(\lim g(x))lim(f(x)g(x))=(limf(x))(limg(x))
4. lim⁡f(x)g(x)=lim⁡f(x)lim⁡g(x)\lim \frac{f(x)}{g(x)} = \frac{\lim f(x)}{\lim g(x)}limg(x)f(x)​=limg(x)limf(x)​, g(x)≠0g(x) \neq 0g(x)=0

---

### **Standard limits**

lim⁡x→0sin⁡xx=1\lim_{x \to 0} \frac{\sin x}{x} = 1x→0lim​xsinx​=1 lim⁡x→01−cos⁡xx=0\lim_{x \to 0} \frac{1 - \cos x}{x} = 0x→0lim​x1−cosx​=0 lim⁡x→∞1x=0\lim_{x \to \infty} \frac{1}{x} = 0x→∞lim​x1​=0

---

### **Limits involving infinity**

- Horizontal asymptotes

lim⁡x→∞1xn=0(n>0)\lim_{x \to \infty} \frac{1}{x^n} = 0 \quad (n > 0)x→∞lim​xn1​=0(n>0)

Example:

lim⁡x→∞3x2+15x2−x=35\lim_{x \to \infty} \frac{3x^2 + 1}{5x^2 - x} = \frac{3}{5}x→∞lim​5x2−x3x2+1​=53​

---

### **Continuity (preview)**

A function is continuous at x=ax=ax=a if:

1. f(a)f(a)f(a) is defined
2. lim⁡x→af(x)\lim_{x \to a} f(x)limx→a​f(x) exists
3. lim⁡x→af(x)=f(a)\lim_{x \to a} f(x) = f(a)limx→a​f(x)=f(a)