### **5.6.1 Definition**

An **advanced function** extends the idea of basic functions and can involve:

- Combinations of functions
- Inverses
- Piecewise definitions
- Exponentials, logarithms, and trigonometric functions

---

### **5.6.2 Types of advanced functions**

**1. Composite function**

- Combining two functions:

(f∘g)(x)=f(g(x))(f \circ g)(x) = f(g(x))(f∘g)(x)=f(g(x))

Example: f(x)=x2,g(x)=x+1f(x) = x^2, g(x) = x + 1f(x)=x2,g(x)=x+1

(f∘g)(x)=f(g(x))=(x+1)2(f \circ g)(x) = f(g(x)) = (x + 1)^2(f∘g)(x)=f(g(x))=(x+1)2

---

**2. Inverse function**

- Reverses the effect of a function:

f(f−1(x))=xf(f^{-1}(x)) = xf(f−1(x))=x

Steps to find f−1(x)f^{-1}(x)f−1(x):

1. Replace f(x)f(x)f(x) with y
2. Swap x and y
3. Solve for y

Example: f(x)=2x+3f(x) = 2x + 3f(x)=2x+3 → y=2x+3y = 2x + 3y=2x+3  
Swap: x=2y+3  ⟹  y=(x−3)/2x = 2y + 3 \implies y = (x-3)/2x=2y+3⟹y=(x−3)/2  
So, f−1(x)=(x−3)/2f^{-1}(x) = (x-3)/2f−1(x)=(x−3)/2

---

**3. Piecewise function**

- Function defined differently in different intervals of x:

Example:

f(x)={x2x≥0−xx<0f(x) = \begin{cases} x^2 & x ≥ 0 \\ -x & x < 0 \end{cases}f(x)={x2−x​x≥0x<0​

---

**4. Exponential and logarithmic functions**

- Exponential: f(x)=axf(x) = a^xf(x)=ax
- Logarithmic: f(x)=log⁡axf(x) = \log_a xf(x)=loga​x
- Inverses of each other

---

**5. Trigonometric functions**

- Sine, cosine, tangent, etc.
- Periodic behavior (repeats after fixed interval)
- Inverse trigonometric functions: sin⁻¹, cos⁻¹, tan⁻¹

---

### **5.6.3 Key properties**

1. Domain and range analysis
2. Monotonicity (increasing/decreasing)
3. Periodicity for trig and exponential
4. Symmetry: even (f(−x)=f(x)) or odd (f(−x)=−f(x))